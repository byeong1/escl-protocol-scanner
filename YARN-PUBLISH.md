# 📦 Yarn으로 npm 레지스트리에 배포하기

## 📚 레지스트리 관점 설명

### npm vs yarn의 관계

```
npm 레지스트리 (https://registry.npmjs.org)
    ↓
    모든 패키지가 여기에 저장됨
    ↓
    ┌─────────────────┬─────────────────┐
    ↓                 ↓
  npm CLI          yarn CLI
  (다운로드)        (다운로드)

둘 다 같은 "npm 레지스트리"에서 패키지를 다운로드합니다!
```

### 핵심 포인트

- **npm 레지스트리**: 패키지가 저장되는 중앙 서버
- **npm 클라이언트**: 패키지 다운로드/업로드 도구
- **yarn 클라이언트**: 패키지 다운로드/업로드 도구

→ **결론**: Yarn을 사용해도 npm 레지스트리에 배포합니다!
(yarn은 npm 레지스트리를 기본으로 사용)

---

## 🚀 Yarn으로 배포하는 방법

### 1단계: npm 계정 생성

npm 웹사이트에서: https://www.npmjs.com/signup

### 2단계: Yarn 로그인

```bash
yarn login
```

또는 npm 로그인:

```bash
npm login
```

**입력:**
- Username
- Password
- Email

### 3단계: 배포

```bash
cd /Users/byeong_il/code/yarn-package/eSCL-Protocol-Scanner

# 빌드
yarn build

# npm 레지스트리에 배포
yarn publish --access public
```

---

## 📋 Yarn vs npm 명령어 비교

| 작업 | npm | yarn |
|------|-----|------|
| 로그인 | `npm login` | `yarn login` |
| 배포 | `npm publish` | `yarn publish` |
| 버전 업데이트 | `npm version minor` | `yarn version --minor` |
| 정보 조회 | `npm view pkg` | `yarn info pkg` |
| 권한 설정 | `npm publish --access public` | `yarn publish --access public` |

---

## 🎯 완전한 배포 워크플로우 (Yarn 사용)

### 첫 번째 배포

```bash
cd /Users/byeong_il/code/yarn-package/eSCL-Protocol-Scanner

# 1. 로그인
yarn login

# 2. 빌드
yarn build

# 3. 배포
yarn publish --access public
```

### 이후 업데이트

```bash
# 1. 코드 수정
# ...

# 2. 버전 업데이트
yarn version --minor   # 1.0.0 → 1.1.0

# 3. 빌드
yarn build

# 4. GitHub 푸시
git add .
git commit -m "v1.1.0: 새로운 기능"
git push

# 5. 배포
yarn publish
```

---

## ✅ 실제 배포 단계

```bash
# 1단계: 로그인
yarn login
# → 사용자명, 비밀번호, 이메일 입력

# 2단계: 빌드 확인
yarn build
# → dist/ 폴더에 JavaScript 생성되는지 확인

# 3단계: 배포 실행
yarn publish --access public

# 4단계: 배포 확인
yarn info @escl-protocol/scanner
```

**배포 결과:**
```
https://www.npmjs.com/package/@escl-protocol/scanner
```

---

## 🔍 다른 프로젝트에서 사용

npm이든 yarn이든 똑같이 사용:

### npm 사용자
```bash
npm install @escl-protocol/scanner
```

### yarn 사용자
```bash
yarn add @escl-protocol/scanner
```

### 둘 다
```bash
pip install zeroconf pillow
```

---

## 🎨 Yarn 전용 명령어

### Yarn으로 버전 관리

```bash
# 패치 버전 (1.0.0 → 1.0.1)
yarn version --patch

# 마이너 버전 (1.0.0 → 1.1.0)
yarn version --minor

# 메이저 버전 (1.0.0 → 2.0.0)
yarn version --major

# 프리릴리스 (1.0.0 → 1.0.0-rc.0)
yarn version --prerelease
```

### Yarn으로 패키지 정보 조회

```bash
# 패키지 정보
yarn info @escl-protocol/scanner

# 특정 필드
yarn info @escl-protocol/scanner version
yarn info @escl-protocol/scanner versions
```

---

## 📊 배포 흐름도

```
┌─────────────────────────────────────┐
│  코드 작성 및 수정                   │
└────────────┬────────────────────────┘
             ↓
┌─────────────────────────────────────┐
│  yarn build (TypeScript 컴파일)      │
└────────────┬────────────────────────┘
             ↓
┌─────────────────────────────────────┐
│  yarn version --minor (버전 업)     │
└────────────┬────────────────────────┘
             ↓
┌─────────────────────────────────────┐
│  git add . && git commit && git push │
│  (GitHub 저장소에 푸시)              │
└────────────┬────────────────────────┘
             ↓
┌─────────────────────────────────────┐
│  yarn publish --access public       │
│  (npm 레지스트리에 배포)             │
└────────────┬────────────────────────┘
             ↓
┌─────────────────────────────────────┐
│  ✅ npm 패키지 생성 완료             │
│  https://npmjs.com/package/...      │
└─────────────────────────────────────┘
```

---

## 🔐 보안 팁

### API 토큰 사용 (선택사항, 더 안전함)

```bash
# 1. npm 웹사이트에서 토큰 생성
# https://www.npmjs.com/settings/tokens

# 2. .npmrc 파일에 저장
cat > ~/.npmrc << EOF
//registry.npmjs.org/:_authToken=YOUR_NPM_TOKEN
EOF

# 3. Yarn으로 배포
yarn publish --access public
```

### 2단계 인증 설정 (권장)

npm 웹사이트 > 계정 설정 > 2단계 인증 활성화

---

## 💡 팁

### 로컬에서 먼저 테스트

```bash
# 글로벌 설치
yarn global add /Users/byeong_il/code/yarn-package/eSCL-Protocol-Scanner

# 또는 npm으로
npm install -g .

# 테스트
node -e "const pkg = require('@escl-protocol/scanner'); console.log(pkg.VERSION)"
```

### 배포 실패 시 확인사항

```bash
# 로그인 상태 확인
yarn config list

# npm 로그인 상태 확인
npm whoami

# 패키지명 이용 가능 여부 확인
yarn info @escl-protocol/scanner 2>/dev/null || echo "사용 가능"
```

---

## 🎯 바로 배포하기 (한번에)

```bash
# 한 줄 명령어 (최초 배포)
cd /Users/byeong_il/code/yarn-package/eSCL-Protocol-Scanner && \
yarn login && \
yarn build && \
yarn publish --access public

# 한 줄 명령어 (업데이트)
cd /Users/byeong_il/code/yarn-package/eSCL-Protocol-Scanner && \
yarn version --minor && \
yarn build && \
git add . && \
git commit -m "Release v$(cat package.json | grep version | head -1 | sed 's/[^0-9.]*//g')" && \
git push && \
yarn publish
```

---

## ✨ 최종 확인 체크리스트

- [ ] npm 계정 생성됨
- [ ] `yarn login` 완료
- [ ] `yarn build` 성공
- [ ] GitHub 저장소 준비됨 (✅ 완료)
- [ ] `yarn publish --access public` 준비됨
- [ ] npm 웹사이트에서 패키지 확인 예정

**준비 완료! 이제 `yarn login`을 실행하고 배포하세요!** 🚀

